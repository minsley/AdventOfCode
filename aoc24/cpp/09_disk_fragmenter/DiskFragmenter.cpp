//
// Created by Matt Insley on 12/10/24.
//

#include "DiskFragmenter.h"

bool DiskFragmenter::solveEq(int left, const std::vector<int> &right) {
    return false;
//    return left == right[0] + blah || left == right[0] * blah;
}

int DiskFragmenter::solve1(const InputSet &input) {
    int sum = 0;
//    for (const auto &eq : input) {
//        if(solveEq(eq) > 0) {
//            sum += eq.left;
//        }
//    }
    return sum;
}