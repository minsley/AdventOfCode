//
// Created by Matt Insley on 12/10/24.
//

#include "HoofIt.h"

bool HoofIt::solveEq(int left, const std::vector<int> &right) {
    return false;
//    return left == right[0] + blah || left == right[0] * blah;
}

int HoofIt::solve1(const InputSet &input) {
    int sum = 0;
//    for (const auto &eq : input) {
//        if(solveEq(eq) > 0) {
//            sum += eq.left;
//        }
//    }
    return sum;
}